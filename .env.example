# Optional: if set, /api/wp-members proxies to this WordPress endpoint.
# If unset, /api/wp-members serves local mock users.
WP_MEMBERS_ENDPOINT=

# Local relay seed script target.
LOCAL_RELAY_URL=ws://127.0.0.1:7011

# Optional deterministic timestamp base for demo events (unix seconds).
# DEMO_SEED_BASE_TS=1735689600

# Browser-side relay target for forum sync.
PUBLIC_NOSTR_RELAY_URL=ws://127.0.0.1:7011
